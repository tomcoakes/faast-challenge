require 'coach'

describe Coach do

  let(:passenger) {double :passenger}
  let(:coach) {Coach.new}

  # it "should hold passengers" do
  #   allow(passenger).to receive(:board_coach).and_return(passenger)
  #   expect(passenger.board_coach).to eq([passenger])
  # end

end